---
- name: Docker Compose stop containers and remove images
  community.docker.docker_compose_v2:
    project_src: "{{ mtproxy_conf_dir }}"
    files:
      - docker-compose.yml
    state: absent
    remove_orphans: true
    remove_images: all
    renew_anon_volumes: true

- name: Purge mtproxy directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  with_items:
    - "{{ mtproxy_conf_dir }}"
    - "{{ mtproxy_data_dir }}"
