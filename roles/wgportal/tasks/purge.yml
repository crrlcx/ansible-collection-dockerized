---
- name: Docker Compose stop containers and remove images
  community.docker.docker_compose_v2:
    project_src: "{{ wgportal_conf_dir }}"
    files:
      - docker-compose.yml
    state: absent
    remove_orphans: true
    remove_images: all
    renew_anon_volumes: true

- name: Purge wgportal directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  with_items:
    - "{{ wgportal_conf_dir }}"
    - "{{ wgportal_data_dir }}"
